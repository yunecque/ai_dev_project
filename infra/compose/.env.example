# Development-only placeholders. Copy to .env and adjust locally.
# Never commit a real .env or production secrets.

POSTGRES_USER=sdlc
POSTGRES_PASSWORD=sdlc-dev-password
POSTGRES_DB=requests

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin-dev-password

GRAFANA_ADMIN=admin
GRAFANA_ADMIN_PASSWORD=admin-dev-password
